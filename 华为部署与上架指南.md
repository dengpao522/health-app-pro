# 华为云函数部署 + APP 备案 + 华为应用市场上架 完整指南

## 第一部分：部署华为云函数（约 15 分钟）

### 1. 登录华为云
- 打开 https://console.huaweicloud.com/functiongraph/
- 用你的华为云账号登录（已实名认证）

### 2. 创建函数
1. 左侧菜单 → **函数** → **创建函数**
2. 选择 **创建空白函数**
3. 填写：
   - 函数名称：`health-ai`
   - 运行时：**Node.js 18.15**
   - 函数类型：**HTTP 函数**
   - 内存规格：**256 MB**（AI 调用足够，省钱）
   - 执行超时：**60 秒**
4. 点击 **创建函数**

### 3. 上传代码
1. 进入函数详情 → **代码** 标签
2. 代码来源选择 **本地上传 ZIP**
3. 上传文件：选择桌面 `health-ai-huawei-function.zip`
4. 确认入口文件为 `index.js`，执行入口为 `index.handler`
5. 点击 **部署**

### 4. 配置环境变量
1. 函数详情 → **配置** 标签 → **环境变量**
2. 添加：
   - 变量名：`DASHSCOPE_API_KEY`
   - 变量值：`sk-你的阿里云百炼API密钥`（与 Cloudflare Worker 用的同一个）
   - 可选：`QWEN_MODEL` = `qwen-vl-plus`（不填则默认此值）
3. 保存

### 5. 配置触发器（获取公网 URL）
1. 函数详情 → **触发器** 标签 → **创建触发器**
2. 触发器类型：**内置 HTTP 触发器**
3. 认证方式：**无需认证**（开放访问，由代码内 Origin 校验控制）
4. 创建后，复制触发器的 **调用 URL**，形如：
   `https://{xxxxx}.apig.cn-north-4.myhuaweicloudapis.com/health-ai`

### 6. 测试函数
用 curl 测试（替换为你的 URL）：
```bash
curl -N -X POST https://你的URL/health-ai \
  -H "Content-Type: application/json" \
  -H "Origin: https://zjh-health.cn" \
  -d '{"image":"data:image/png;base64,iVBORw0KGgoAAAANSUhEUg..."}'
```
应返回 SSE 流：`data: {"type":"meta",...}`

---

## 第二部分：APP 备案（约 3-4 周，最大卡点）

APP 备案是工信部强制要求，由**境内网络接入服务商**代为提交。
你的华为云函数使用华为云作为接入商，因此通过华为云提交备案。

### 备案前提
- 华为云账号已实名认证 ✅（你已完成）
- 域名 `zjh-health.cn` 已完成 **ICP 备案**（网站备案）
  - 如果域名还没做 ICP 备案，需要先在华为云做 ICP 备案（约 20 工作日）
  - 已有 ICP 备案的话，APP 备案可以复用主体信息

### 备案步骤
1. 进入华为云备案系统：https://beian.huaweicloud.com/
2. 选择 **APP 备案** → **新增 APP 备案**
3. 填写 APP 信息：
   - APP 名称：**健康饮食助手**
   - APP 包名：`com.dengpao522.healthpro`
   - 运行平台：Android
   - 接入商：华为云（函数服务）
   - 域名/IP：填写你的华为云函数调用 URL 域名
4. 上传材料：
   - 主办者身份证正反面
   - APP 功能说明（拍照识别食物、营养计算、健康管理）
   - 隐私政策链接
5. 提交后，华为云初审（1-3 工作日）→ 管局审核（20 工作日内）

### 备案通过后
- 获取 APP 备案号，形如 `京ICP备XXXXXXXX号-1A`
- 在 APP 内"关于"页面标注备案号
- 在华为应用市场上架时填写备案号

---

## 第三部分：华为应用市场上架（审核 1-3 工作日）

### 前提
- APP 备案号 ✅（第二部分完成后获得）
- APK 包（已用自有证书签名）✅（桌面 健康饮食助手.apk）

### 上架步骤
1. 登录 AppGallery Connect：https://developer.huawei.com/consumer/cn/service/josp/agc/
2. **我的应用** → **新建应用**
3. 填写应用信息：
   - 应用名称：健康饮食助手
   - 应用包名：com.dengpao522.healthpro
   - 应用分类：健康 > 健康管理
4. **版本信息**：
   - 上传 APK（桌面的 健康饮食助手.apk）
   - 填写版本号 1.0.0
   - 填写更新说明
5. **应用信息**：
   - 应用介绍（拍照识别食物成分、智能营养计算、科学塑形指导）
   - 上传应用截图（5 张以上）
   - 上传应用图标
   - 隐私政策 URL
6. **版权信息**：
   - APP 备案号：填写第二部分获得的备案号
   - 软著：选填（你的软著申请下来后再补充）
7. **AI 功能声明**：
   - 声明使用了生成式 AI（通义千问）用于图片识别
   - 大模型服务方：阿里云（已备案）
8. 提交审核，等待 1-3 工作日

---

## 关键时间预估

| 环节 | 预计耗时 | 阻塞项 |
|------|---------|--------|
| 部署华为云函数 | 15 分钟 | 需你登录操作 |
| ICP 备案（域名） | 20 工作日 | 如域名未备案 |
| APP 备案 | 20 工作日 | 需 ICP 备案完成 |
| 华为审核 | 1-3 工作日 | 需 APP 备案号 |
| **总计** | **约 4-5 周** | |

## 已准备好的文件
- `huawei-function/index.js` — 华为云函数代码（已验证可用）
- `huawei-function/package.json` — 依赖声明
- `health-ai-huawei-function.zip` — 部署包（桌面）
